# Egitim yigini — SURUMLER SABIT.
#
# Neden sabit: TRL'in SFT API'si bu projenin yazildigi donemde degisti.
# `max_seq_length` -> `max_length` oldu ve varsayilani 1024'e indi; eski adi
# veren script hata VERMEZ, sessizce 1024'te calisir ve her ornegi keser.
# Bu tur bir sapmayi kosu bittikten sonra fark etmek, kiralik GPU saatini
# ve olcumun tamamini cope atmak demektir.
#
# Asagidaki surumler 2026-07-31'de dogrulandi: src/train_lora.py bu yigin
# uzerinde CPU smoke testinden gecti (kayip maskesi 108/2294 token = %4.7,
# yani completion_only_loss beklendigi gibi calisiyor).
#
# Kurulum (kiralik pod'da):
#   pip install -r requirements-train.txt
#   # torch'u pod'un CUDA surumune gore AYRICA kurun — asagidaki liste
#   # bilerek torch icermiyor, cunku dogru tekerlek makineye baglidir.

transformers==5.14.1
trl==1.9.2
peft==0.20.0
datasets==5.0.1
accelerate==1.14.0

# QLoRA (--4bit) icin; yalnizca dar VRAM'de gerekir, CPU'da kurulmaz.
# Colab/Kaggle'da SABIT SURUMLE kuruluyor (not defteri 2. hucre, COLAB.md §2):
#   bitsandbytes==0.50.0
# Denetim 2026-08-05: bu paket tek pinsiz dogrudan bagimlilikti. Pin, o gunun
# "en son"u ile AYNI surum — yani kurulani degistirmiyor, yalnizca donduruyor.
# Burada yorumda kalmasinin sebebi degismedi: bu dosya CPU'da da kuruluyor.

# ---------------------------------------------------------------------------
# 🔴 TORCHAO — kurmak degil, KALDIRMAK gerekir (olculdu 2026-08-01, Colab T4)
#
# Bu liste torchao ICERMEZ ve gerekmiyor. Ama ONCEDEN KURULU bir torchao,
# peft 0.20.0'in LoRA dispatcher'inda kosuyu OLDURUYOR:
#
#   peft/tuners/lora/model.py  -> dispatch_torchao()
#   peft/import_utils.py       -> is_torchao_available()
#   ImportError: Found an incompatible version of torchao.
#                Found version 0.10.0, but only versions above 0.16.0 are supported
#
# Kritik ayrinti: `is_torchao_available()` eski surumde False DONMUYOR,
# ImportError ATIYOR. Yani "torchao kullanmiyoruz" korumuyor — dispatcher
# her LoRA katmani icin cagiriyor ve SFTTrainer kurulurken patliyor.
#
# Neden yerelde gorunmedi: bu makinede torchao HIC kurulu degil, o yuzden
# find_spec None donuyor ve fonksiyon temiz sekilde False veriyor. CPU smoke
# testi bu yuzden gecti. Colab imajinda torchao 0.10.0 HAZIR geliyor.
# ⇒ Ortam farki, kod farki degil: "yerelde gecti" bunu YAKALAMAZ.
#
# Cozum (Colab/pod'da kurulumdan SONRA):
#   pip uninstall -y torchao        # kullanmiyoruz, en dar duzeltme
#   # ya da: pip install -U "torchao>0.16"   (daha buyuk indirme, torch'u
#   #        zorlayabilir — gerekmedikce tercih etmeyin)
